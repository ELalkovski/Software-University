This program receives a sequence of strings, each on a new line. Every odd line on the console representing a resource (e.g. Gold, Silver, Copper, and so on), and every even – quantity.
The program collect the resources and prints them each on a new line. 
