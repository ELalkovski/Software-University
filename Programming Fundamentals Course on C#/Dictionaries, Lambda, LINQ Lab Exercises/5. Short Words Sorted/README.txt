This program reads a text, extracts its words, finds all short words (less than 5 characters) and prints them alphabetically, in lowercase.
•	Uses the following separators for the input: . , : ; ( ) [ ] " ' \ / ! ? (space).
•	Uses case-insensitive matching.
•	Removes duplicated words.
